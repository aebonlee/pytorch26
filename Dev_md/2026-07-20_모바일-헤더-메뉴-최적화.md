# 2026-07-20 · 모바일 헤더 메뉴 최적화 (햄버거 통일)

## 문제

모바일에서 상단 메뉴가 깨져 보임.

원인은 단순하다. 헤더 메뉴가 **12개**(About·선수학습·환경설정·1~3일차·요약정리·실습소스·프로젝트·복습퀴즈·심화학습·VOD)인데
`.nav`가 `display:flex`에 `flex-wrap` 없이 한 줄로만 놓여 있었고,
`.site-header .inner`는 `height: 60px` **고정**이었다.
좁은 폭에서 메뉴가 로고를 밀어내거나 헤더 밖으로 삐져나온다.
기존 대응은 `@media (max-width:640px)`에서 글자·패딩을 줄이는 것뿐이라 근본 해결이 아니었다.

## 최종 결론 — 좁은 폭은 햄버거 하나로 통일

| 폭 | 동작 |
|---|---|
| 1101px~ | 기존 그대로 한 줄 전부 노출 |
| ~1100px | **햄버거 버튼** + 그리드 드롭다운. 헤더는 60px 한 줄 유지 |

### 반려된 두 방식 (다시 시도하지 말 것)

1. **메뉴만 2줄로 줄바꿈** — 로고 옆에 2줄짜리 메뉴 덩어리가 붙어 **로고 영역까지 2줄로 보인다.**
2. **헤더를 위·아래로 분리**(윗줄 로고+토글 / 아랫줄 메뉴 전체폭) — 헤더가 두꺼워져 반려.

→ 폭이 좁으면 예외 없이 접는다. 대표 확정(2026-07-20).

### src/components/Layout.jsx

- 메뉴 12개를 `MENU` 배열로 뽑고 `map`으로 렌더 (JSX 12줄 나열 → 배열 1곳 관리)
- `open` 상태 추가. 햄버거는 `.nav-toggle`, 막대 3개는 `<i>` 3개로 그리고 열리면 X자로 회전
- **테마 토글은 드로어 안에 넣지 않고** `.header-actions`로 빼서 헤더 바에 계속 노출.
  이유: 테마 전환하려고 메뉴를 여는 건 불필요한 한 단계다
- 닫히는 경로 4가지: 메뉴 클릭(`<nav>` onClick) / 경로 변화(`useLocation`) / ESC / 스크림 탭
  - 경로 변화만으로는 **같은 메뉴 재클릭** 때 안 닫힌다 → onClick을 함께 둠
  - 반대로 onClick만으로는 로고 클릭 이동이 안 잡힌다 → 둘 다 필요
- `aria-expanded` / `aria-controls="site-nav"` / `aria-label` 부여

### src/styles/global.css

- `.header-actions`, `.nav-toggle`, `.bars` 신규
- `@media (max-width:1100px)` — `.nav`를 `position:absolute; top:100%` 드롭다운으로.
  열 수는 `repeat(auto-fill, minmax(150px, 1fr))`로 **폰 2열 → 태블릿 5~6열 자동**.
  `1fr 1fr` 고정이면 태블릿에서 항목 하나가 화면 절반을 먹어 우스워진다
- 기존 640 블록의 `.nav a` 축소 규칙은 불필요해져 제거, `.logo .sub` 숨김은 유지

## 밟은 함정 (같은 실수 반복 방지)

- **드롭다운 배경에 `--header-bg`를 쓰면 안 된다.** 이 값은 반투명(0.92)이라 뒤 본문 글자가 비쳐 읽히지 않는다. 불투명 `--bg` 사용.
- 드롭다운을 `position:absolute`로 헤더에 붙일 수 있는 건 `.site-header`가 `position:sticky`(=위치 지정 요소)이기 때문. 이 sticky를 풀면 드롭다운이 페이지 최상단으로 날아간다.
- 스크림 `z-index:40` — 헤더(50)보다 **낮아야** 한다. 높으면 메뉴 자체가 스크림에 덮여 클릭이 안 먹는다.
- 화면 폭 판정은 **CSS 미디어쿼리에만** 맡겼다. JS로 `window.innerWidth`를 재면 리사이즈·화면 회전 때 상태가 어긋난다. `open`은 "열려는 의도"만 뜻하고 데스크톱에선 CSS가 `.nav`를 항상 보이게 해서 무해하다.
- `.bars i`의 transition을 `prefers-reduced-motion` 블록에 추가.
  이유: 이 사이트는 2026-07-17에 Windows 애니메이션 끄기 설정에서 화면이 멎은 이력이 있다(`/check.html` 진단 페이지 참고). 새 애니메이션을 넣을 때마다 이 블록도 같이 갱신한다.
- 수정 후 화면이 그대로면 **배포 전이거나 CSS 캐시**다. 강력 새로고침으로 먼저 확인.

## 검증

- `npm run build` 통과 (55 modules, CSS 21.31kB)
- Actions 배포 success
- 화면 확인은 대표가 직접 (§5-5)

---

## 세션 종료 (2026-07-20)

### 최종 상태

- 브랜치 `main`, origin과 동기화, 작업 트리 깨끗함
- 오늘 커밋 4건 — 전부 푸시·배포 완료

| 커밋 | 내용 |
|---|---|
| `3689adc` | 모바일 헤더 메뉴 깨짐 — 2줄 줄바꿈 + 햄버거 드롭다운 |
| `aec34d6` | 헤더 2줄 분리 (윗줄 로고 / 아랫줄 메뉴) — **이후 반려** |
| `909f971` | **좁은 폭은 햄버거 하나로 통일 (~1100px)** ← 확정본 |
| `dd731d5` | 개발일지 |

중간 두 방식이 커밋으로 남아 있는 이유는 시행착오 경로를 지우지 않기 위해서다.
**현재 살아 있는 동작은 `909f971`(햄버거 통일) 하나뿐**이다.

### 미확인 사항 (열린 채로 종료)

- 실제 폰·태블릿 실기기 확인을 대표가 진행하기로 하고 세션 종료.
  개발 쪽 검증(빌드·배포)은 끝났으나 **실기기 육안 확인은 미완**이다.
  이상이 있으면 이 문서의 "밟은 함정"부터 확인할 것.

### 강의 전(2026-07-27~29) 재점검 예정 항목

세션 중 제안했고 대표가 "강의 전 재점검" 하기로 한 목록. 아직 **아무것도 수행하지 않았다.**

1. 콘텐츠 정합성 — 3일차 커리큘럼·실습소스·퀴즈 데이터를 `vite-node`로 직접 로드해 대조 (§5.1)
2. 외부 링크 전수 점검 — 패들렛·VOD·심화학습
3. 실습 코드 실행 검증 — 현재 PyTorch 버전에서 동작하는지
4. 모바일 본문 점검 — 헤더 외 표·코드블록·도해가 폰에서 깨지지 않는지
5. 진도 저장 안내 — localStorage 기반이라 브라우저·기기가 바뀌면 초기화된다. 수강생 안내 문구 필요 여부 판단

### 참고

- 이 리포 `Dev_md`는 번호식(`01_`~`25_`)과 날짜식이 섞여 있다.
  `2026-07-19`부터 날짜식이므로 신규 문서는 날짜식으로 통일한다(전역 §5-2와 동일).
